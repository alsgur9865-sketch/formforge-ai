# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
dist/
build/

# Secrets / credentials (절대 commit 금지)
.env
.env.local
.env.*.local
service-account.json
*.json.key
secrets/

# IDE
.vscode/
.idea/
*.code-workspace
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Test / coverage
.pytest_cache/
.coverage
htmlcov/

# Uploaded videos (개발용. 운영은 Cloud Storage. 어느 경로든 ignore)
*.mp4
*.mov
*.avi
!data/sample_videos/.gitkeep
# 데모 영웅 스켈레톤 영상은 데모 자산이라 추적(빌드/재현성)
!data/demo_skeleton.mp4

# MediaPipe 모델 자산 (바이너리 ~9MB. git 추적 X — 다운로드 가능)
#   다운로드: analyze_video() 에러 메시지의 curl 명령 참조
#   (pose_landmarker_full.task @ storage.googleapis.com/mediapipe-models/...)
data/models/*.task

# Cloud Run / build artifacts
.dockerignore.bak
*.log

# Streamlit cache
.streamlit/secrets.toml

# Claude Code local metadata (sessions, plans, locks)
.claude/
